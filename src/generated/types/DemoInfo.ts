/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { DemoDataset } from './DemoDataset'
import type { DemoTier } from './DemoTier'
import type { DemoUpgrade } from './DemoUpgrade'
import type { DemoWalkthroughStep } from './DemoWalkthroughStep'

/**
 * @description Everything an unauthenticated caller needs to decide whether to ask for a demo credential: whether the demo is up, how much capacity is left, what the tiers cost, what the dataset holds, and the walkthrough to run.
 * @type object
*/
export type DemoInfo = {
    /**
     * @description Whether a credential can be issued right now. False when the deployment has no demo database configured or the global ceiling is full.
     * @type boolean
    */
    available: boolean;
    /**
     * @description Why no credential can be issued. Present only when available is false.
     * @example not_configured
     * @type string | undefined
    */
    unavailable_reason?: string;
    /**
     * @description Seconds until the oldest live session expires and capacity frees up. Present only when the ceiling is full.
     * @type integer | undefined
    */
    retry_after_seconds?: number;
    /**
     * @description Grades of demo credential this deployment offers.
     * @type array
    */
    tiers: DemoTier[];
    /**
     * @description The fictional dataset a demo credential reads. It is owned by PgBeam, shared by every concurrent demo, and read-only, so no session can change what another session sees.
     * @type object
    */
    dataset: DemoDataset;
    /**
     * @description Statements to run, in order, to observe each guardrail.
     * @type array
    */
    walkthrough: DemoWalkthroughStep[];
    /**
     * @description Demo credentials currently issued and unexpired.
     * @type integer
    */
    live_sessions: number;
    /**
     * @description Hard ceiling on concurrent live demo credentials across the whole deployment. Vending stops at this number.
     * @type integer
    */
    max_live_sessions: number;
    /**
     * @description Credentials one client address may be issued per rolling hour. Counted against a keyed hash of the address, never the address itself.
     * @type integer
    */
    max_sessions_per_hour_per_client: number;
    /**
     * @description What to do with the demo once it has proved the point. The policy that governed the demo is handed back as a document that can be posted verbatim to the real policy endpoint, so the evaluation carries into the account.
     * @type object
    */
    upgrade: DemoUpgrade;
};
