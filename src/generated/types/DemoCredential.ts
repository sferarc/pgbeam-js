/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { DemoDataset } from './DemoDataset'
import type { DemoUpgrade } from './DemoUpgrade'
import type { DemoWalkthroughStep } from './DemoWalkthroughStep'
import type { PolicyProfileInput } from './PolicyProfileInput'

/**
 * @description A working, scoped, read-only demo credential against PgBeam\'s own database, plus everything needed to use it and to reproduce its policy on a real project. The connection string and MCP token are shown once and are not retrievable again.
 * @type object
*/
export type DemoCredential = {
    /**
     * @description Identifier for this demo session. Quote it when releasing the credential, and it is carried on the signup link.
     * @example dmo_01h455vb4pex5vsknk084sn02q
     * @type string
    */
    session_id: string;
    /**
     * @description Tier that was issued.
     * @type string
    */
    tier: string;
    /**
     * @description Guarded PostgreSQL connection string. TLS is mandatory and SNI selects the project, so the host must not be replaced with an IP address.
     * @type string
    */
    connection_string: string;
    /**
     * @description Hosted MCP endpoint for the same guarded database.
     * @type string
    */
    mcp_url: string;
    /**
     * @description Bearer token for the MCP endpoint. Shown once.
     * @type string
    */
    mcp_token: string;
    /**
     * @description When the credential stops working. Past this instant the proxy refuses new connections and terminates live sessions.
     *
     * Format: `date-time`
     * @type string
    */
    expires_at: string;
    /**
     * @description Seconds from issue to expiry.
     * @type integer
    */
    ttl_seconds: number;
    /**
     * @description Statements this credential may run before it is refused.
     * @type integer
    */
    query_budget: number;
    /**
     * @description The fictional dataset a demo credential reads. It is owned by PgBeam, shared by every concurrent demo, and read-only, so no session can change what another session sees.
     * @type object
    */
    dataset: DemoDataset;
    /**
     * @description Statements to run, in order, to observe each guardrail.
     * @type array
    */
    walkthrough: DemoWalkthroughStep[];
    /**
     * @description Mutable fields of a policy profile (used for create and update).
     * @type object
    */
    policy_template: PolicyProfileInput;
    /**
     * @description What to do with the demo once it has proved the point. The policy that governed the demo is handed back as a document that can be posted verbatim to the real policy endpoint, so the evaluation carries into the account.
     * @type object
    */
    upgrade: DemoUpgrade;
};
