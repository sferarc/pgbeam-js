/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

/**
 * @description The organization\'s plan limits and marginal overage rates, resolved for both of the zero meanings a limit carries. Supplied as context for a caller that wants to price usage itself; this endpoint deliberately prices nothing.
 * @type object
*/
export type AgentUsageMarginalRates = {
    /**
     * @description Plan name the rates come from.
     * @type string
    */
    plan: string;
    /**
     * @description Daily query limit. Zero means no limit at all, not a limit of zero.
     *
     * Format: `int64`
     * @type integer
    */
    queries_per_day: number;
    /**
     * @description Monthly transfer limit in bytes. Zero means no limit at all.
     *
     * Format: `int64`
     * @type integer
    */
    bytes_per_month: number;
    /**
     * @description Rate per 1000 queries past the limit. Zero means the plan is a hard cap with no overage purchased, which is a different thing from no limit.
     *
     * Format: `double`
     * @type number
    */
    overage_per_1k_queries: number;
    /**
     * @description Rate per GB past the limit. Zero means a hard cap with no overage.
     *
     * Format: `double`
     * @type number
    */
    overage_per_gb: number;
    /**
     * @description True only when a query limit exists and overage past it was purchased. Both gates resolved, so a caller cannot reproduce the bug where an organization with unlimited queries had its whole usage metered.
     * @type boolean
    */
    query_overage_billable: boolean;
    /**
     * @description True only when a bytes limit exists and overage past it was purchased.
     * @type boolean
    */
    bytes_overage_billable: boolean;
};
