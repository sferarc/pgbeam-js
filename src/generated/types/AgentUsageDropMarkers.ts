/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

/**
 * @description Evidence that the trail lost entries in this window. Nothing about the surviving rows looks wrong when this happens, so it is reported explicitly.
 * @type object
*/
export type AgentUsageDropMarkers = {
    /**
     * @description Number of gap markers read, capped at 1000.
     * @type integer
    */
    markers: number;
    /**
     * @description True when the window held more gap markers than the cap, so entries_dropped accounts for only the ones read and the real gap is larger. Reported rather than left to look like an exact count.
     * @type boolean
    */
    truncated: boolean;
    /**
     * @description Entries the markers account for. A floor: it counts only markers whose count could be read.
     *
     * Format: `int64`
     * @type integer
    */
    entries_dropped: number;
    /**
     * @description Markers whose count could not be read, so their entries are missing from entries_dropped. Reported rather than assumed zero.
     * @type integer
    */
    markers_unparsed: number;
    /**
     * @description Distinct marker reasons, sorted, so an operator can tell the two buffers apart.
     * @type array
    */
    reasons: string[];
};
