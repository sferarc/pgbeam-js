/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { BatchOperationResult } from './BatchOperationResult'
import type { BatchSummary } from './BatchSummary'

export const batchResponseModeEnum = {
    atomic: "atomic",
    independent: "independent"
} as const;

export type BatchResponseModeEnumKey = (typeof batchResponseModeEnum)[keyof typeof batchResponseModeEnum];

/**
 * @description The outcome of a batch. `results` carries one entry per requested operation in the order they were sent, and `summary` is the same information counted so a caller can branch on one field instead of scanning. Results deliberately do not carry the mutated resources: a batch reports what happened to each item, and a caller that needs the resource reads it, which keeps the response size a function of the item count alone and keeps one response shape for every operation kind.\n
 * @type object
*/
export type BatchResponse = {
    /**
     * @description The failure semantics this batch ran under, echoed back.
     * @type string
    */
    mode: BatchResponseModeEnumKey;
    /**
     * @description Whether any of the batch\'s writes are in effect. False only for an atomic batch that rolled back, which is also answered with 409.\n
     * @type boolean
    */
    applied: boolean;
    /**
     * @description Counts of the per-operation outcomes in a batch.
     * @type object
    */
    summary: BatchSummary;
    /**
     * @description One result per requested operation, in request order.
     * @type array
    */
    results: BatchOperationResult[];
};
