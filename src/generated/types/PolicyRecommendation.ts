/**
* Generated by Kubb (https://kubb.dev/).
* Do not edit manually.
*/

import type { PolicyProfileInput } from './PolicyProfileInput'
import type { PolicyReplayResult } from './PolicyReplayResult'

/**
 * @description A least-privilege policy derived from an agent credential\'s recorded audit history, together with the replay diff that proves the candidate would not newly block any statement the credential legitimately ran. Advisory only: this endpoint never creates, updates, or otherwise mutates a policy or a credential. A good recommendation has replay.summary.newly_blocked == 0.
 * @type object
*/
export type PolicyRecommendation = {
    /**
     * @description True when there was enough parseable, executed traffic to derive a candidate. When false, candidate and replay are omitted and message explains why no recommendation could be made.
     * @type boolean
    */
    sufficient_history: boolean;
    /**
     * @description Present when sufficient_history is false; a clear explanation of why no recommendation could be produced (e.g. no executed traffic in the window).
     * @type string | undefined
    */
    message?: string;
    /**
     * @description Mutable fields of a policy profile (used for create and update).
     * @type object | undefined
    */
    candidate?: PolicyProfileInput;
    /**
     * @description The outcome of replaying recorded agent traffic against a candidate policy: an aggregate summary plus per-query decisions, changes first.
     * @type object | undefined
    */
    replay?: PolicyReplayResult;
    /**
     * @description Distinct executed query shapes the candidate was derived from (blocked attempts are excluded from derivation).
     * @type integer
    */
    observed_queries: number;
    /**
     * @description Recorded audit entries covered by the analyzed window.
     *
     * Format: `int64`
     * @type integer
    */
    observed_entries: number;
    /**
     * @description Start of the analyzed traffic window (inclusive).
     *
     * Format: `date-time`
     * @type string
    */
    window_start: string;
    /**
     * @description End of the analyzed traffic window (exclusive).
     *
     * Format: `date-time`
     * @type string
    */
    window_end: string;
    /**
     * @description Human-readable rationale for each tightening decision (e.g. why the candidate was downgraded to read-only, or how max_rows was chosen).
     * @type array | undefined
    */
    notes?: string[];
};
